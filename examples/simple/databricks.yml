bundle:
  name: webhook-test-bundle

targets:
  dev:
    mode: development
    default: true

resources:
  jobs:
    webhook_test_job:
      name: webhook-test-job
      tags:
        webhook-test: simple
      tasks:
        - task_key: fail_me
          notebook_task:
            notebook_path: ./notebooks/fail_me.py
